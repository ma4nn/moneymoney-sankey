// chart-library specific selectors, centralized so a library switch only touches this file
export const chartSelectors = {
    tooltip: '.highcharts-tooltip',
    container: '.highcharts-container',
    strongTextClass: 'highcharts-strong', // class the chart library renders for <strong> tooltip markup
};
